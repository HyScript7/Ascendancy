# This file was generated by the Gradle 'init' task.
# https://docs.gradle.org/current/userguide/platforms.html#sub::toml-dependencies-format
[versions]
minecraft = "26.1.2"
paper = "26.1.2.build.+"
# In case we ever decide to test...
# junit = "5.10.0"

carbon = "3.0.0-beta.37"
citizens = "2.0.42-SNAPSHOT"
sentinel = "2.9.4-SNAPSHOT"
if = "0.12.0"

[libraries]
paper = { module = "io.papermc.paper:paper-api", version.ref = "paper" }
# In case we ever decide to test...
# junit = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }

carbon = { module = "de.hexaoxi:carbonchat-api", version.ref = "carbon" }
citizens = { module = "net.citizensnpcs:citizens-main", version.ref = "citizens" }
sentinel = { module = "org.mcmonkey:sentinel", version.ref = "sentinel" }
if = { module = "com.github.stefvanschie.inventoryframework:IF", version.ref = "if" }

[bundles]
server = ["paper"]
client = []
chat = ["carbon"]
npc = ["citizens", "sentinel"]
menu = ["if"]
